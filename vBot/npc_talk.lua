onAttackingCreatureChange(function(creature, OldCreature)
    if creature and creature:isNpc() and creature:getName() == "nn" and distanceFromPlayer(creature:getPosition()) <= 3 then
        CaveBot.Conversation("hi", "trade")
     elseif creature and creature:isNpc() and creature:getName() == "nn" and distanceFromPlayer(creature:getPosition()) <= 3 then
        CaveBot.Conversation("hi", "trade")
        elseif creature and creature:isNpc() and creature:getName() == "nn" and distanceFromPlayer(creature:getPosition()) <= 3 then
        CaveBot.Conversation("hi", "trade")
  end
end)
